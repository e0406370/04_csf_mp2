export interface Place {

  venue: string;
  address: string;
  latitude: number;
  longitude: number;
};

export interface CreationDetails {

  name: string;
  description: string;
  logo: string;
  start: string;
  end: string;
}